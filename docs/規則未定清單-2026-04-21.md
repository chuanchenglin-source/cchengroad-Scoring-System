# 🕹️ 規則未定清單（2026-04-21 主辦方提問）

**來源**：2026-04-21 上午 08:54 主辦方在 LINE 群組 @All 提出的 8 個規則細項問題
**用途**：追蹤哪些問題待主辦方確認、哪些已有答案、哪些已寫入 `scoring_rules` 表
**相關 change**：`openspec/changes/add-scoring-rules-and-role-access/`

---

## 問題 ↔ rule_key ↔ 狀態對照表

| # | 主辦方原文 | `rule_key` | 狀態 | `rule_value`（已 seed）|
|---|---|---|---|---|
| Q1 | 第七週還有領袖通話加分嗎？還是改成只有第八週呢？ | `w7_leadership_bonus_enabled` | 🟡 待主辦方確認 | `NULL` |
| Q2a | 全完成是指全部（定課+主題）都完成嗎？只要每個人填回報分數都達到那週最高分就算嗎？ | `full_completion_definition` | 🟡 待主辦方確認 | `NULL` |
| Q2b | 是加入整組團隊總分嗎？ | `team_bonus_target` | ✅ 已答：每人個別加 | `"individual"` |
| Q2c | 團隊半數認列，如果 7 人隊完成 3 人算過半嗎？還是一定要 4 人？要無條件進位，還是一定要剛好過半？ | `half_completion_method` | 🟡 待主辦方確認 | `NULL` |
| Q3a | 團隊排名，要計算總分，還是團隊平均分？因為每隊人數不一致 | `team_ranking_method` | ✅ 已答：平均分 | `"average"` |
| Q3b | 小隊長和大隊長是否要計算入團隊總分？ | `include_leaders_in_average` | 🟡 待主辦方確認 | `NULL` |
| Q3c | 審計官如果發現有缺件，要如何扣分，或是補件？ | `audit_missing_item_action` | 🟡 待主辦方確認 | `NULL` |
| Q3d | 傳愛的名單確認，要如何回報和找誰確認？ | _(未建 rule_key)_ | 🟠 需求層級問題 | — |

---

## 狀態說明

- ✅ **已答**：主辦方已明確回答，`rule_value` 已 seed 預設值
- 🟡 **待主辦方確認**：`rule_value = NULL`，Dashboard 呼叫 `get_rule()` 拿到 NULL 時應顯示「規則未設定」
- 🟠 **需求層級問題**：不是單純改參數，需要開新功能（傳愛名單回報）。留給未來 `add-angel-assignment-tracking` change 處理

---

## 主辦方回答後，如何更新

假設主辦方確認「7 人隊 3 人算過半（無條件進位）」：

```sql
UPDATE scoring_rules
   SET rule_value = '"ceil"'::jsonb,
       updated_at = now()
 WHERE activity_id = 1 AND rule_key = 'half_completion_method';
```

假設主辦方確認「小隊長計入、大隊長不計入」：

```sql
UPDATE scoring_rules
   SET rule_value = '{"squad_leader": true, "team_leader": false}'::jsonb,
       updated_at = now()
 WHERE activity_id = 1 AND rule_key = 'include_leaders_in_average';
```

假設主辦方確認「審計缺件預設請補件、不直接扣分」：

```sql
UPDATE scoring_rules
   SET rule_value = '{"default_action": "request_supplement", "deduct_amount": 0}'::jsonb,
       updated_at = now()
 WHERE activity_id = 1 AND rule_key = 'audit_missing_item_action';
```

---

## 對主辦方的建議提問順序

如果要主動追主辦方，建議按**對系統影響程度**排序：

1. **Q2c（半數定義）** & **Q3b（隊長計入）** — 這兩題卡住排名與團隊加分計算
2. **Q3c（缺件處理）** — 影響審計官 Dashboard 設計（需要顯示「請補件」按鈕嗎？）
3. **Q1（W7 領袖通話）** — 影響 W7 計分邏輯
4. **Q2a（全完成定義）** — 影響團隊動能加分判定
5. **Q3d（傳愛名單）** — 影響是否要開新功能，可能需要另一次會議討論

---

## 相關檔案

- SQL seed 腳本：`scripts/supabase-import/06-scoring-rules.sql`
- 讀規則 helper function：`scripts/supabase-import/06-scoring-rules.sql` 內 `get_rule(activity_id, rule_key)`
- Spectra change：`openspec/changes/add-scoring-rules-and-role-access/`
