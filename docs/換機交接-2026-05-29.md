# 換機交接note — 2026-05-29

給新機器接手的 Claude / Johnson 看。讀完這份就能無縫接手。

---

## 目前在做什麼

**偵查官扣分機制**（雙系統設計）。完整設計討論結論見 `docs/偵查官扣分機制-設計討論-2026-05-29.md`。

兩個系統：
- **計分系統**（顯示扣分）— 本 repo（baseline）+ fork
- **偵查官系統**（輸入扣分）— 另一個 repo，本機未 clone

---

## ⚠️ 三個 repo / 程式碼來源（重要，別搞混）

| 名稱 | 路徑 | 角色 | git remote |
|---|---|---|---|
| **baseline** | `cchengroad-Scoring System` | 14-box 舊版（正式環境快照）| ✓ 有 GitHub remote |
| **fork** | `cchengroad-Scoring-System-fork-0527` | **40-box 新版，副本 GAS 實際跑的** | ❌ **無 remote** |
| 偵查官 | （本機沒有）| 偵查官系統 | 未知 |

**關鍵**：副本計分系統 GAS（`1iBj0Wl3`）跑的是 **fork（40-box）**，不是 baseline。詳見 memory `reference-test-environment-0527`。

---

## ✅ 已完成（偵查官扣分「顯示」端）

1. **設計討論 + spectra change**（在 baseline）：`openspec/changes/add-deduction-display-in-history`（proposal/design/specs/tasks 齊全、已驗證）
2. **baseline 實作**（commit 已 push）：14-box 版，**只當設計參考**（副本不是跑這個）
3. **fork 實作**（commit `692d96b`，本機獨有，但**程式碼已 clasp push 到副本 GAS**）：
   - `config.js`：加 `SS_ID_MONITOR_DEDUCTION = '1_bkaXc...'`、`SHEET_NAME_DEDUCTION`
   - `webApp.js`：`getPersonalHistory` 跨 Sheet 讀 active 扣分 + `_readActiveDeductions` helper（box 1-41、graceful failure）
   - `history.html`：紅色扣分列、`calculateSummary` 扣除、escapeHtml、mock 示範
4. 偵查官副本 Sheet（`1_bkaXc`）已建「扣分記錄」分頁 + 12 欄

---

## ⏳ 進行中 / 待辦

### A. 副本驗收（fork 扣分顯示）— 還沒跑
副本 GAS 已有程式碼。需要：
1. 在偵查官 Sheet `1_bkaXc` 的「扣分記錄」分頁加測試扣分（被扣人ID 要用副本裡真的有填分的人）
2. 開副本 history.html 看紅色扣分列 + 總分扣除
3. 前提：副本計分 Sheet（`1YNB7s`）的「每日個人分數結算」要有資料

### B. 除錯中：偵查官小隊選單顯示「T011 小隊」
- **現象**：偵查官「搜尋小隊」選單顯示「T011 小隊」，正式環境顯示真實小隊名
- **已查證**：副本兩張 Sheet 的人員總表「小隊名」欄**資料正常**（有「嘉5！嘉起來」等真實名）→ **不是資料問題**
- **推斷**：偵查官顯示邏輯沒讀「小隊名」欄，或欄位索引對不上副本 Sheet 排列（`1YNB7s` 8 欄 vs `1R-0b` 9 欄，小隊名位置不同）
- **卡住**：偵查官 code 不在本機，需 clone 偵查官 repo 才能定位+修
- **還需要**：圖2 正式環境截圖、偵查官讀哪張 Sheet

### C. 偵查官端 spectra change（寫入扣分流程）— 未開
草稿在 `docs/偵查官端-spectra-change-草稿-2026-05-29.md`（注意：要改成 40-box）

---

## 🖥️ 新機器接手步驟

### 1. baseline（git）
```bash
cd <新機器>/cchengroad-Scoring System
git fetch origin
git checkout chore/restore-baseline-2026-05-27
git pull
```

### 2. fork（無 git remote → 用 clasp 從副本 GAS 拉）
```bash
cd <新機器某資料夾>
mkdir cchengroad-Scoring-System-fork-0527 && cd cchengroad-Scoring-System-fork-0527
# 載入 fnm（Windows）
eval "$(fnm env)" 2>/dev/null
# clasp clone 副本計分系統 GAS（含偵查官扣分功能）
npx clasp clone 1iBj0Wl3UKPJrLErt5iqQ_NEwmDEMKntQO35WkyrEpjr8RVUgsP0crdfR
```
> ⚠️ 新機器若沒 clasp，注意別裝 v3（會壞）。可從 baseline 的 node_modules 借 v2，或 `clasp login` 先登入。

### 3. 偵查官 repo（要做除錯 B 才需要）
clone 偵查官 repo（網址問 Johnson / 主工程師）

### 4. 開 Claude Code，說「按照 docs/換機交接-2026-05-29.md 接手」

---

## 副本環境 5 個 ID（完整見 memory `reference-test-environment-0527`）

| 角色 | ID |
|---|---|
| 計分系統副本 GAS（跑 fork）| `1iBj0Wl3UKPJrLErt5iqQ_NEwmDEMKntQO35WkyrEpjr8RVUgsP0crdfR` |
| 計分系統副本 Sheet（SS_ID_MAIN）| `1YNB7s-JjPRurGTrgAjPU0qwVyeSvyLo5c5I-K3uA1Gk` |
| 填分回應副本 Sheet（SS_GROUP_LOG）| `1R-0b_mcy97IbH8r485_BV6gUHJIjmDT-AFRk91zAGDU` |
| 偵查官副本 GAS | `1UGg3htqYDVRjJBewGdvNpZsj4fjmuSLSxvC9_3o_e5SSYfbVbkvFiI3-` |
| 偵查官副本 Sheet（扣分記錄）| `1_bkaXcApXPKe9I1kF0DL4gne06e__8YiC5yy5FOgT3Y` |

---

## 紅線提醒
- 正式環境絕不碰（計分 Sheet `1CFTaHN`、計分 GAS `19ZCMtO64`、偵查官 GAS `1y2NcmR9`）
- fork 的 config 直接寫副本 ID（沒有 dev/prod 切換），部署正式時要連同所有 ID 一起換
